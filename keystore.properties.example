# Copy to keystore.properties locally if needed. Do NOT commit real passwords.
# CI uses env vars instead: VAULT_KEYSTORE_FILE, VAULT_KEYSTORE_PASSWORD, VAULT_KEY_ALIAS, VAULT_KEY_PASSWORD
storeFile=../vault-release.jks
storePassword=REPLACE_ME
keyAlias=vault
keyPassword=REPLACE_ME
